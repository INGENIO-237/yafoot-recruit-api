export enum ENV {
  DEV = "development",
  PROD = "production",
}

export enum HTTP {
  ACCEPTED = 202,
  OK = 200,
  CREATED = 201,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  SERVER_ERROR = 500,
}